{
  "name": "@cacao-colab/stripe-client",
  "version": "0.1.0",
  "private": true,
  "description": "Stub de Stripe Connect Express — SIN credenciales ni cuenta creada todavía. Ver docs/08-PAGOS.md. No instanciar el cliente real hasta tener STRIPE_SECRET_KEY.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "stripe": "^22.3.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
